/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig
module.exports = {
    images: {
      domains: ['example.com'], // Add your domain(s) where the images are hosted
    },
  };
